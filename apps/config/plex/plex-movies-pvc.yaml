apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-movies-pvc 
  namespace: plex              
spec:
  volumeName: plex-movies-pv
  accessModes:
    - ReadWriteMany         
  resources:
    requests:
      storage: 500Gi       