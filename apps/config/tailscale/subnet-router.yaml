apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: local-subnet-router
  namespace: tailscale
spec:
  hostname: local-subnet-router
  proxyClass: default-resources
  subnetRouter:
    advertiseRoutes:
      - "192.168.1.0/24"